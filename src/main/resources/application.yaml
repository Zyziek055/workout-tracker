spring:
  application:
    name: workout-tracker
  datasource:
    url: jdbc:mysql://localhost:3306/workout_tracker_api?createDatabaseIfNotExist=true
    username: root
    password: rootroot
  jpa:
    hibernate:
      ddl-auto: validate
  jwt:
    secret: ${JWT_SECRET}
  flyway:
    enabled: true


